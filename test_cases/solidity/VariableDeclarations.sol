// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract VariableDeclarations {
    function compute(uint64 x, uint64 y) public pure returns (uint64) {
        uint64 sum = x + y;
        bool ok = true;
        uint64 result = sum;
        return sum;
    }
}
